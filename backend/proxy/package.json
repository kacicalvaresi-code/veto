{
  "name": "veto-proxy",
  "version": "2.0.0",
  "description": "Veto production backend — spam database pipeline and privacy-preserving proxy",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build-db": "node -e \"require('./spamBuilder').buildSpamDatabase().then(n => { console.log('Built:', n); process.exit(0); }).catch(e => { console.error(e); process.exit(1); })\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["spam", "robocall", "privacy", "ios"],
  "author": "Veto",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.6",
    "csv-parse": "^5.5.3",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "express-rate-limit": "^7.1.5",
    "node-cron": "^3.0.3"
  }
}
